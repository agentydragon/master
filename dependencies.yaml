dependencies:
  org.apache.beam:
    beam-runners-google-cloud-dataflow-java:
      # version: "2.1.0"  # = beam.version
      # version: "2.3.0"  # = beam.version
      lang: java

  org.apache.hbase:
    hbase-client:
      # version: "1.4.2"
      lang: java

  com.google.cloud:
    google-cloud-storage:
      # version: "1.14.0"
      lang: java

      # TODO: pin to 1.22.0
      #  com.google.api-client:
      #    google-api-client:
      #      version: "1.22.0"  # pin
      #      lang: java

  com.google.cloud.bigtable:
    bigtable-hbase-2.x:
      # version: "1.3.0" -- downgrade due to https://github.com/GoogleCloudPlatform/DataflowJavaSDK/issues/607
      version: "1.1.2"
      lang: java
    bigtable-hbase-beam:
      version: "1.1.2"  # = bigtable.version
      lang: java

  info.bliki.wiki:
    bliki-core:
      version: "3.1.0"
      lang: java
