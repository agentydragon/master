0. Select relevant articles
	bazel run //prototype:find_persons | tee persons

1. Split Wikipedia
	bazel run //prototype/split_wiki:launch

2. Add Spotlight to relevant articles
	bazel run //prototype/add_spotlight:launch -- --article_list_file=persons --max_articles=5

3. Add parses to relevant articles
	bazel run //prototype/add_parses:launch -- --article_list_file=persons --max_articles=5

4. Make training samples
	bazel run //prototype/make_training_samples -- 'George Washington'
		%-- TODO FIX SAMPLES

5. Join positive samples
	bazel run //prototype/make_training_samples:join_positives

5. Make negative samples
	bazel run //prototype/make_training_samples:make_negative_samples
