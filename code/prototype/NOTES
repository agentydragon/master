0. Select relevant articles
	bazel run //prototype:find_persons | tee persons

1. Split Wikipedia
	bazel run //prototype/split_wiki:launch

2. Add Spotlight to relevant articles
	bazel run //prototype/entity_recognition:launch -- --max_articles=5

3. Add parses to relevant articles
	bazel run //prototype/parse:launch -- --max_articles=5

4. Make training samples for distant supervision (both positive and negative)
	bazel run //prototype/make_training_samples
